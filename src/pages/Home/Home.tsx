import { Navbar } from '../../components/Navbar';
import { HeroSection } from '../../components/HeroSection';

export const Home = () => {
	return (
		<header>
			<Navbar />
			<HeroSection />
		</header>
	);
};
